frontend-test:

  stage: test

  script:

    - cd Frontend

    - npm ci --cache .npm

    - npm test -- --watch=false

    - npm run build

  artifacts:

    when: always

    paths:

      - Frontend/dist

  rules:

    - when: always